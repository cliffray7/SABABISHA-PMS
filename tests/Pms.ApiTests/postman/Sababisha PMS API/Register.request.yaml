$kind: http-request
url: "{{baseUrl}}/auth/register"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "firstName": "Postman",
      "lastName": "User",
      "email": "{{email}}",
      "password": "{{password}}"
    }
order: 1000
