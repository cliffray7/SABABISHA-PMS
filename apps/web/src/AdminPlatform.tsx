import { useState } from 'react';
import { Alert, Box, Button, Card, CardContent, CircularProgress, Typography } from '@mui/material';
import { useQuery } from '@tanstack/react-query';
import { api } from './api';

export function AdminActivity() { return <Box sx={{ p:3 }}><Typography variant="h4" component="h1">Activity</Typography><Typography color="text.secondary" sx={{ mt:1, mb:3 }}>Platform audit activity.</Typography><Alert severity="info">TaskFlow does not currently store a persistent platform audit log. This page intentionally does not present sample activity as real data.</Alert></Box>; }
export function AdminHealth() { const [checkedAt, setCheckedAt] = useState<string>(); const query = useQuery({ queryKey:['admin-health'], queryFn:async () => { const [apiStatus, databaseStatus] = await Promise.all([api.get('/health'), api.get('/ready')]); setCheckedAt(new Date().toLocaleString()); return { api:apiStatus.data.status, database:databaseStatus.status }; }, retry:false }); return <Box sx={{ p:3 }}><Typography variant="h4" component="h1">System health</Typography><Typography color="text.secondary" sx={{ mt:1, mb:3 }}>Current availability of TaskFlow services.</Typography><Button variant="outlined" onClick={() => void query.refetch()} disabled={query.isFetching}>Refresh</Button>{query.isFetching && <CircularProgress size={20} sx={{ ml:2 }}/>} {query.error && <Alert severity="error" sx={{ mt:3 }}>Unable to check the current health status.</Alert>}{query.data && <Box sx={{ display:'flex', gap:2, flexWrap:'wrap', mt:3 }}>{[['API', query.data.api],['Database', query.data.database]].map(([label,status]) => <Card key={label} sx={{ minWidth:210 }}><CardContent><Typography color="text.secondary">{label}</Typography><Typography variant="h6">{status}</Typography></CardContent></Card>)}</Box>}{checkedAt && <Typography variant="body2" color="text.secondary" sx={{ mt:2 }}>Last checked: {checkedAt}</Typography>}</Box>; }
export function AdminSettings() { return <Box sx={{ p:3 }}><Typography variant="h4" component="h1">Platform settings</Typography><Typography color="text.secondary" sx={{ mt:1, mb:3 }}>Safe platform information.</Typography><Card><CardContent><Typography variant="subtitle2">Application</Typography><Typography>TaskFlow</Typography><Typography variant="subtitle2" sx={{ mt:2 }}>Environment</Typography><Typography>{import.meta.env.MODE}</Typography><Typography variant="body2" color="text.secondary" sx={{ mt:2 }}>Theme is managed per browser from the header. Security credentials and runtime configuration are intentionally not editable here.</Typography></CardContent></Card></Box>; }
